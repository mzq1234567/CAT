# Report assets

Drop the TechPlus Talent logo here as **`tpt_logo.png`** (PNG with transparent
background works best; it is placed on the dark cover page and in the page header).

The report renders fine without it — the cover falls back to a text wordmark — but
adding the file makes the generated PDF match the branded client template.
